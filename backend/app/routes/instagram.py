from flask import Blueprint, request, jsonify, redirect, url_for\nfrom flask_jwt_extended import jwt_required, get_jwt_identity\nfrom app.services.instagram_oauth import InstagramOAuthService\nfrom app.utils.exceptions import APIException, ValidationError, NotFoundError\nimport logging\nimport secrets\nfrom flask import session\n\nlogger = logging.getLogger(__name__)\ninstagram_bp = Blueprint('instagram', __name__)\n\n@instagram_bp.route('/authorize', methods=['POST'])\n@jwt_required()\ndef get_instagram_auth_url():\n    \"\"\"Get Instagram authorization URL - الحصول على رابط التفويض\"\"\"\n    try:\n        user_id = get_jwt_identity()\n        \n        # Generate state for CSRF protection\n        state = secrets.token_urlsafe(32)\n        session[f'instagram_state_{user_id}'] = state\n        \n        auth_url = InstagramOAuthService.get_authorization_url(state)\n        \n        return jsonify({\n            'success': True,\n            'auth_url': auth_url,\n            'message': 'Redirect user to this URL to authorize'\n        }), 200\n    \n    except APIException as e:\n        return jsonify({\n            'success': False,\n            'error': e.message\n        }), e.status_code\n    except Exception as e:\n        logger.error(f'Error getting auth URL: {str(e)}')\n        return jsonify({\n            'success': False,\n            'error': 'Failed to get authorization URL'\n        }), 500\n\n@instagram_bp.route('/callback', methods=['GET'])\ndef instagram_callback():\n    \"\"\"Instagram OAuth callback - رد الاتصال من Instagram\"\"\"\n    try:\n        code = request.args.get('code')\n        state = request.args.get('state')\n        error = request.args.get('error')\n        \n        # Check for errors\n        if error:\n            error_description = request.args.get('error_description', 'Unknown error')\n            logger.error(f'Instagram OAuth error: {error} - {error_description}')\n            return redirect(f'http://localhost:3000/auth/instagram-error?error={error}')\n        \n        if not code:\n            return redirect('http://localhost:3000/auth/instagram-error?error=no_code')\n        \n        # Get user_id from JWT token if available, otherwise from session\n        # In a real app, you'd extract this from the session or JWT\n        user_id = request.args.get('user_id')\n        \n        if not user_id:\n            return redirect('http://localhost:3000/auth/instagram-error?error=no_user')\n        \n        # Verify state for CSRF protection\n        stored_state = session.get(f'instagram_state_{user_id}')\n        if not stored_state or stored_state != state:\n            logger.warning(f'State mismatch for user {user_id}')\n            return redirect('http://localhost:3000/auth/instagram-error?error=state_mismatch')\n        \n        # Exchange code for token\n        account = InstagramOAuthService.exchange_code_for_token(code, int(user_id))\n        \n        # Clear state from session\n        session.pop(f'instagram_state_{user_id}', None)\n        \n        # Redirect to frontend with success\n        return redirect(f'http://localhost:3000/accounts?connected=true&account_id={account.id}')\n    \n    except Exception as e:\n        logger.error(f'Instagram callback error: {str(e)}')\n        return redirect('http://localhost:3000/auth/instagram-error?error=callback_failed')\n\n@instagram_bp.route('/accounts', methods=['GET'])\n@jwt_required()\ndef get_instagram_accounts():\n    \"\"\"Get all Instagram accounts - الحصول على جميع الحسابات\"\"\"\n    try:\n        user_id = get_jwt_identity()\n        \n        accounts = InstagramOAuthService.get_user_accounts(user_id)\n        \n        return jsonify({\n            'success': True,\n            'accounts': [account.to_dict() for account in accounts]\n        }), 200\n    \n    except Exception as e:\n        logger.error(f'Error getting accounts: {str(e)}')\n        return jsonify({\n            'success': False,\n            'error': 'Failed to get accounts'\n        }), 500\n\n@instagram_bp.route('/accounts/<int:account_id>', methods=['GET'])\n@jwt_required()\ndef get_instagram_account(account_id):\n    \"\"\"Get specific Instagram account - الحصول على حساب معين\"\"\"\n    try:\n        user_id = get_jwt_identity()\n        \n        account = InstagramOAuthService.get_account(account_id, user_id)\n        \n        return jsonify({\n            'success': True,\n            'account': account.to_dict()\n        }), 200\n    \n    except NotFoundError as e:\n        return jsonify({\n            'success': False,\n            'error': e.message\n        }), 404\n    except Exception as e:\n        logger.error(f'Error getting account: {str(e)}')\n        return jsonify({\n            'success': False,\n            'error': 'Failed to get account'\n        }), 500\n\n@instagram_bp.route('/accounts/<int:account_id>/primary', methods=['POST'])\n@jwt_required()\ndef set_primary_account(account_id):\n    \"\"\"Set as primary account - تعيين كحساب أساسي\"\"\"\n    try:\n        user_id = get_jwt_identity()\n        \n        account = InstagramOAuthService.set_primary_account(account_id, user_id)\n        \n        return jsonify({\n            'success': True,\n            'message': 'Primary account set successfully',\n            'account': account.to_dict()\n        }), 200\n    \n    except NotFoundError as e:\n        return jsonify({\n            'success': False,\n            'error': e.message\n        }), 404\n    except Exception as e:\n        logger.error(f'Error setting primary account: {str(e)}')\n        return jsonify({\n            'success': False,\n            'error': 'Failed to set primary account'\n        }), 500\n\n@instagram_bp.route('/accounts/<int:account_id>/refresh-token', methods=['POST'])\n@jwt_required()\ndef refresh_token(account_id):\n    \"\"\"Manually refresh access token - تحديث الـ access token يدويًا\"\"\"\n    try:\n        user_id = get_jwt_identity()\n        \n        # Verify account belongs to user\n        account = InstagramOAuthService.get_account(account_id, user_id)\n        \n        # Refresh token\n        InstagramOAuthService.refresh_access_token(account_id)\n        \n        updated_account = InstagramOAuthService.get_account(account_id, user_id)\n        \n        return jsonify({\n            'success': True,\n            'message': 'Token refreshed successfully',\n            'account': updated_account.to_dict()\n        }), 200\n    \n    except NotFoundError as e:\n        return jsonify({\n            'success': False,\n            'error': e.message\n        }), 404\n    except APIException as e:\n        return jsonify({\n            'success': False,\n            'error': e.message\n        }), e.status_code\n    except Exception as e:\n        logger.error(f'Error refreshing token: {str(e)}')\n        return jsonify({\n            'success': False,\n            'error': 'Failed to refresh token'\n        }), 500\n\n@instagram_bp.route('/accounts/<int:account_id>/disconnect', methods=['POST'])\n@jwt_required()\ndef disconnect_account(account_id):\n    \"\"\"Disconnect Instagram account - قطع الاتصال\"\"\"\n    try:\n        user_id = get_jwt_identity()\n        \n        # Verify account belongs to user\n        account = InstagramOAuthService.get_account(account_id, user_id)\n        \n        # Revoke access\n        InstagramOAuthService.revoke_access(account_id)\n        \n        return jsonify({\n            'success': True,\n            'message': 'Account disconnected successfully'\n        }), 200\n    \n    except NotFoundError as e:\n        return jsonify({\n            'success': False,\n            'error': e.message\n        }), 404\n    except APIException as e:\n        return jsonify({\n            'success': False,\n            'error': e.message\n        }), e.status_code\n    except Exception as e:\n        logger.error(f'Error disconnecting account: {str(e)}')\n        return jsonify({\n            'success': False,\n            'error': 'Failed to disconnect account'\n        }), 500\n"